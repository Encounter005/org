:PROPERTIES:
:ID:       56cedcd7-e6e7-46b2-8935-fc5ffff689dd
:END:
#+title: python-function
#+startup: overview
#+filetags: :python:function:

* 容器拆包
就是把一个元组的数据一个一个拆解出来
#+begin_src python :results output 
tuple1 = (10, 20)
num1, num2 = tuple1
print(f'{tuple1}\n {num1}, {num2}')
a, b = (20, 30)
print(f'{a}, {b}')
# swap
c1 = 'hello'
c2 = 'nihao'
c2, c1 = c1, c2
print(f'{c1}, {c2}')
#+end_src

* 推导式
推导式是可以从一个数据序列构建另一个新的数据序列（一个有规律的列表或控制一个有规律列表）的结构体。 共有三种推导：列表推导式、集合推导式、字典推导式。

** 列表推导式
#+begin_src
变量名 = [表达式 for 变量 in 列表]
变量名 = [表达式 for 变量 in 列表 if 条件]
变量名 = [表达式 for 变量 in 列表 for 变量 in 列表]
#+end_src

*** 正常推导式
#+begin_src python :results output
# 初始化一个1~10的列表
list1 = [i for i in range(1,11)]
print(list1)

# 给定一个列表，将这个列表加入到另一个集合中，实现去重
my_list2 = [7, 9, 5, 4, 5, 7, 6]
my_set = {e for e in my_list2}
print(my_set)
#+end_src
*** 列表推导式+条件判断
#+begin_src python :results output
# 生成0-9之间的偶数序列
my_list = [e for e in range(1, 10) if e % 2 == 0]
print(my_list)
#+end_src
*** for循环嵌套
#+begin_src python :results output
# 生成一个坐标表
grids = [(row, col) for row in range(3) for col in range(4)]
print(grids)

seats = []

# 外层循环：遍历每一行
for row in range(3):
    # 内层循环：遍历每一列
    for col in range(4):
        seat = (row, col)
        seats.append(seat)

print(seats)
#+end_src

#+RESULTS:
: [(0, 0), (0, 1), (0, 2), (0, 3), (1, 0), (1, 1), (1, 2), (1, 3), (2, 0), (2, 1), (2, 2), (2, 3)]
: [(0, 0), (0, 1), (0, 2), (0, 3), (1, 0), (1, 1), (1, 2), (1, 3), (2, 0), (2, 1), (2, 2), (2, 3)]

* 函数调用
#+begin_src
# 定义函数
def 函数名称([参数1, 参数2, ...]):
    函数体
    ...
    [return 返回值]

# 调用函数
函数名称(参数1, 参数2, ...)
#+end_src

#+begin_src python :results output
def size(num1, num2):
    jia = num1 + num2
    jian = num1 - num2
    cheng = num1 * num2
    chu = num1 / num2
    return jia, jian, cheng, chu


# 调用size方法
print(size(20, 5))
#+end_src

#+RESULTS:
: (25, 15, 100, 4.0)
** 递归
计算阶乘
#+begin_src python :results output
def func(num:int):
    if num <= 1:
        return num
    return num * func(num - 1)

print(func(4))
#+end_src

青蛙台阶
#+begin_src python :results output
'''
需求：有一个小青蛙，每次可以跳1个台阶，或者跳2个台阶，总共10个台阶，问有多少种走法
思路：
1. 当仅有一个台阶，那么就是1中走法，fn(1) = 1
2. 当有两个台阶，那么有两种走法,f(2) = 2
3. 当有三个台阶， 那么有这些走法 f(3) = f(2) + f(1)
4. 如果有四级台阶， 那么走法为f(4) = f(3) + f(2)
5. 如果有n级台阶，那么走法为f(n) = f(n-1) + f(n-2)
'''

def func(n:int):
    if n == 1:
        return 1
    elif n == 2:
        return 2
    return func(n - 1) + func(n - 2)

print(func(10))
        
#+end_src

汉诺塔
#+begin_src python :results output
'''
汉诺塔
移动n个盘子，从A--->C盘
思路：
将前n-1个盘子从A柱借助C柱移动到B柱
将第n个盘子从A柱移动到C柱
将前n-1个盘子从B柱借助A柱移动到C柱
'''


def hanuota(num, A,B,C):
    '''
    这是一个汉诺塔函数，表示将num个盘子，从A柱移动到C柱的流程
    :param num: 盘子个数
    :param A:  A柱子，起始柱子
    :param B:  B柱子，辅助柱子
    :param C:  C柱子，目的柱子
    :return: None
    '''
    if num == 1:
        print(f'将第{num}个盘子从{A}移动到{C}')
        return
    # 将num-1个盘子从A借助C移动到B
    hanuota(num-1, A,C,B)
    # 将第num个盘子，从A移动到C
    print(f'将第{num}个盘子从{A}移动到{C}')
    # 将num-1个盘子从B借助A移动到C
    hanuota(num-1,B,A,C)

hanuota(3,'A','B','C')
#+end_src

** 函数参数类型调用  
*** 位置参数
理论上，在函数定义时，我们可以为其定义多个参数。但是在函数调用时，我们也应该传递多个参数，正常情况，其要一一对应。
#+begin_src python :results output
def user_info(name, age, address):
    print(f'我的名字{name}，今年{age}岁了，家里住在{address}')
    
# 调用函数
user_info('Tom', 23, '美国纽约')
#+end_src
*** 关键词参数
函数调用，通过“键=值”形式加以指定。可以让函数更加清晰、容易使用，同时也清除了参数的顺序需求。
#+begin_src python :results output
def user_info(name, age, address):
    print(f'我的名字{name}，今年{age}岁了，家里住在{address}')
    
# 调用函数（使用关键词参数）
user_info(name='Tom', age=23, address='美国纽约')
#+end_src
*** 函数定义时缺省参数（参数默认值）
缺省参数也叫默认参数，用于定义函数，为参数提供默认值，调用函数时可不传该默认参数的值（注意：所有位置参数必须出现在默认参数前，包括函数定义和调用）。
#+begin_src python :results output
def user_info(name, age, gender='男'):
    print(f'我的名字{name}，今年{age}岁了，我的性别为{gender}')


user_info('李林', 25)
user_info('振华', 28)
user_info('婉儿', 18, '女')
#+end_src
*** 不定长参数
不定长参数也叫可变参数。用于不确定调用的时候会传递多少个参数(不传参也可以)的场景。
**** 不定长元组
#+begin_src python :results output
def user_info(*args):
    print(f'my name: {args[0]}, i am {args[1]} years old, living in {args[2]}')
user_info('encounter', 18, 'hello')
#+end_src
**** 不定长字典
#+begin_src python :results output
def user_info(**args):
    print(f'my name: {args['name']}, i am {args['age']} years old, living in {args["address"]}')


user_info(
    name = 'encounter',
    age = 12,
    address = "hello"
)
#+end_src

** lambda函数
#+begin_src 
变量 = lambda 函数参数:表达式（函数代码 + return返回值）
# 调用变量
变量()
#+end_src
#+begin_src python :results output 
def func(str):
    return 'hello' + str

print(func(' world'))

fn = lambda str: 'hello' + str
print(fn(' world'))

fn = lambda num1, num2 = 20: num1 + num2
print(fn(1))

students = [
    {'name': 'Tom', 'age': 20},
    {'name': 'Rose', 'age': 19},
    {'name': 'Jack', 'age': 22}
]

students.sort(key=lambda x: x['age'], reverse=True)
print(students)
#+end_src

#+RESULTS:
: hello world
: hello world
: 21
